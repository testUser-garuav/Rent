/// Core constants used across the app.

// Replace with your backend base URL, e.g. "https://api.example.com".
const String kBaseUrl = String.fromEnvironment('BACKEND_URL', defaultValue: 'https://your-backend-url.com');

// Storage keys
const String kAuthTokenKey = 'auth_token';
